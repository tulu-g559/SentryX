[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sentryx"
version = "0.1.0"
description = "AI-powered Mission Control for local microservices"
dependencies = [
    "typer"
]

[project.scripts]
sentryx = "sentryx.main:app"